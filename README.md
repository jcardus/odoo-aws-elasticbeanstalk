# odoo-aws-elasticbeanstalk
Ready to use elasticbeanstalk environment with odoo 16

Configure VPCId SubnetA and SubnetB in .ebextensions

Use as the repo as a source to elastic beanstalk and set the configuration using env vars
